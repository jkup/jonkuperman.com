---
permalink: /tags/
layout: layouts/base.njk
title: Topics
---
<div class="wrapper">
  <header class="page-header">
    <p class="page-header__eyebrow">{{ collections.tagList | length }} topics</p>
    <h1>Topics</h1>
    <p class="page-header__subtitle">Explore the tools I work with, the things I’m learning, and life along the way.</p>
  </header>

  <div class="tag-cloud">
  {% for tag in collections.tagList %}
    {% set tagUrl %}/tags/{{ tag | slug }}/{% endset %}
    <a href="{{ tagUrl | url }}" class="tag-cloud__tag">{{ tag }} <span class="tag-cloud__count">{{ collections[tag] | length }}</span></a>
  {% endfor %}
  </div>
</div>
